#!/bin/sh
# hello-marker — benign round-trip proof hook (codex block).
# No-op: prints a marker and exits 0. Never blocks a tool, never mutates state.
echo "[hello-marker] tachyon plugin round-trip OK (codex)"
exit 0
