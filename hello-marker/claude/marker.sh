#!/bin/sh
# hello-marker — benign round-trip proof hook (claude block).
# No-op: prints a marker and exits 0. Never blocks a tool, never mutates state.
echo "[hello-marker] tachyon plugin round-trip OK (claude)"
exit 0
