{
  "name": "commit-guard",
  "version": "1.0.1",
  "description": "A demonstrable git pre-commit gate: blocks any commit whose staged diff contains the marker DONOTCOMMIT. Runtime-agnostic — runs for you, the agent, and your IDE; declares no runtime (git-hooks need none).",
  "gitHooks": {
    "pre-commit": { "leaf": "githooks/guard.sh" }
  }
}
