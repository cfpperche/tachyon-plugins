{
  "name": "commit-guard",
  "version": "1.0.0",
  "description": "A demonstrable git pre-commit gate (spec 264 proof): blocks any commit whose staged diff contains the marker DONOTCOMMIT. Runtime-agnostic — runs for you, the agent, and your IDE. Declares claude only to satisfy the manifest's runtime requirement (it ships no runtime block).",
  "runtimes": ["claude"],
  "gitHooks": {
    "pre-commit": { "leaf": "githooks/guard.sh" }
  }
}
