# Credits & attribution

This plugin depends on and adapts material from **HyperFrames** by HeyGen:

- **Project:** `github.com/heygen-com/hyperframes`
- **License:** Apache License 2.0
- **Engine:** the `hyperframes` npm CLI, run pinned via `npx` (not bundled/redistributed here).
- **Adapted:** the minimal composition template (`references/composition-template/` — the `index.html` timeline
  shape, `hyperframes.json` config, and the pinned `package.json`) is adapted from the HyperFrames composition
  conventions. The authoring reference points back to HeyGen's official HyperFrames skills/docs rather than
  duplicating them.

The HyperFrames CLI and its official agent-skills remain the property of HeyGen under Apache-2.0. This plugin is an
independent Tachyon-native wrapper and is not affiliated with or endorsed by HeyGen.
